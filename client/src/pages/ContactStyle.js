import { makeStyles } from "@material-ui/core";

export default makeStyles(()=>({
    wrapper:{
        height:'100vh',
        width:'100vw',
        display:'flex',
        justifyContent:'center',
        alignItems:'center'
    }
}))